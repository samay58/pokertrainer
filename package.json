{
  "name": "poker-trainer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "dev": "pnpm --filter frontend run dev",
    "clean": "pnpm -r run clean",
    "typecheck": "tsc -b",
    "lint": "tsc -b --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
